#include <stdio.h>
#include "main.h"

/**
 * _isalpha - checks if character is a letter
 *
 * Return: int 1 if character is a letter otherwise returns 0
 */
int _isalpha(int c)
{
	int result;

	if ((c >= 'A' && c <= 'A') || (c >= 'a' && ))
		result = 1;
	else
		result = 0;

	return (result);
}
