#include <stdio.h>
#include "main.h"

/**
 * add - adds numbers
 * @a: first number
 * @b: second number
 * Return: always returns sum
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
