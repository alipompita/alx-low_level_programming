#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two numbers
 * @a: first number
 * @b: second number
 * Return: int product of input numbers.
 */
int mul(int a, int b)
{
	int product;

	product = a * b;
	return (product);
}
